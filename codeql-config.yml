name: "AstraGuard CodeQL Configuration"

paths-ignore:
  - tests
  - docs
  - examples
  - htmlcov
  - .github
  - assets

queries:
  - uses: security-extended
  - uses: security-and-quality

disable-default-queries: false

python:
  version: "3.11"
